---
import { whatsappHref } from '../content/site';
---
<div class="sticky-cta" aria-label="Acciones principales">
  <a class="btn btn-primary" href="#versiones" onclick="trackLandingEvent('click_view_pricing', {location: 'sticky'})">Comprar</a>
  <a class="btn btn-secondary" href={whatsappHref()} onclick="trackLandingEvent('click_whatsapp', {location: 'sticky'})">WhatsApp</a>
</div>
